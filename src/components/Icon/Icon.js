import Icon from 'react-native-vector-icons/MaterialIcons';
import withStyles from '@blankapp/ui/src/withStyles';

const mapStyleToProps = [
  'color',
  'size',
];

export default withStyles('Icon', mapStyleToProps)(Icon);
